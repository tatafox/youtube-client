import { ColorByDateDirective } from './color-by-date.directive';

describe('ColorByDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorByDateDirective();
    expect(directive).toBeTruthy();
  });
});
